{
  "name": "bowler-iq-server",
  "version": "1.0.0",
  "description": "BolwerIQ backend API",
  "engines": {
    "node": "6.9.x"
  },
  "scripts": {
    "start": "node server",
    "heroku-postbuild": "cd react-ui/ && npm install --only=dev && npm install && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "react-ui/node_modules"
  ],
  "dependencies": {
    "express": "^4.14.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/aautem/bowler-iq.git"
  },
  "license": "MIT",
  "devDependencies": {}
}
